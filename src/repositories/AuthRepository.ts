export class AuthRepository {
    static async Login(username:string, password:string) {
        //TODO Usar esta funcion para la peticion a "api/auth/login", y preferentemente hacer el proceso de almacenar el token aca tambien.
    }
}